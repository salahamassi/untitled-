import 'package:flutter/material.dart';

extension Middleware on Widget {
  dynamic middleware(List<String> middlewares) {

    // 1- find the middleware type
    // 2- create an instance from this middle ware
    // 3- return the result of middleware this this
    return this;
  }
}
